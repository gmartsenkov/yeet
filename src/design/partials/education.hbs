<div class="text-black">
    <div class="text-2xl mb-5 pl-3">Education</div>
    <hr>
    <div class="mt-3 px-5 divide-y">
        {{#each data}}
            <div class="py-3">
                <div class="font-bold text-lg">{{degree}}</div>
                <div class="font-light text-gray-800">{{institution}}</div>
                <div class="mt-2">
                    {{#if start_date}} {{start_date}} {{/if}}
                    {{#if end_date}} - {{end_date}} {{/if}}
                </div>
            </div>
        {{/each}}
    </div>
</div>